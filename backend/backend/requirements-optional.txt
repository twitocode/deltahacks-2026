# Optional dependencies for advanced features
#
# These are not required for basic operation but enable additional functionality:
#
# For GeoTIFF elevation data support:
# rasterio>=1.3.0
# GDAL>=3.6.0  (requires system GDAL installation)
#
# For geometric operations:
# shapely>=2.0.0
#
# Install with: pip install -r requirements-optional.txt
#
# Note: GDAL/rasterio can be tricky to install. On macOS:
#   brew install gdal
#   pip install rasterio
